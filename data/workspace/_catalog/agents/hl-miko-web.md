# Agente `hl-miko-web` 🛠️

**Descripción:** Desarrollo web hl_miko — HL-Go backend, APIs, permisos.

## Qué hace

- Monorepo **hl_miko** (HealthLog / Miko), rama `dev.h-l.cl`
- Backend PHP: `HL-Go/` (APIs, permisos, usuarios)
- Docs: `docs-ia-local/`, `instructions.md`, `TareasProximas.csv`
- Máximo 3 `exec` por turno; cambios mínimos alineados al estilo del repo

## Workspace

`/home/node/.openclaw/workspace/projects/hl_miko`  
Detalle: `projects/hl_miko/AGENTS.md`, `projects/hl_miko/TOOLS.md`

## Relación con hlgo

Mismo repo físico; **hlgo** enfocado en logística/QA Playwright, **hl-miko-web** en desarrollo web general.
